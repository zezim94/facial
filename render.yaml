
services:
  - type: web
    name: reconhecimento-facial
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: seu_banco_render
          property: connectionString
